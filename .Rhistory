devtools::document()
devtools::check()
devtools::document()
devtools::check()
devtools::check()
devtools::check()
usethis::use_package("magrittr", type = "Imports")
devtools::check()
devtools::document()
devtools::check()
devtools::check()
devtools::check()
devtools::check()
devtools::check()
devtools::load_all(".")
library(SurvivalFast)
canser
?canser
survivalFast(canser$Week,            # 時間(必填)
canser$Censord,         # 設限(必填)，需定義為：1 = event；0 = censor
canser$Group,           # 分組(必填)，接受兩組以上
y = 8,                  # 時間點，估計該時間點的統計量
conf.level = 0.95,      # 信心水準，計算信賴區間。預設為0.95
conf.type = "log",      # 計算信賴區間方式：plain；log，預設為plain。
hazard.type = "Nelson", # 計算累積風險函數方式：likelihood；Nelson，預設為likelihood。
plot = TRUE,            # 顯示圖表與否。預設為TRUE
plot.conf = TRUE,       # 顯示信賴區間與否。預設為TRUE
statistics = TRUE)
devtools::check()
devtools::check()
devtools::check()
devtools::check()
devtools::load_all(".")
devtools::load_all(".")
library(SurvivalFast)
canser
survivalFast(canser$Week,canser$Censord,canser$Group,y = 8,conf.level = 0.95,conf.type = "log",hazard.type = "Nelson",plot = TRUE,plot.conf = TRUE,statistics = TRUE)
survivalFast(canser$Week,canser$Censord,canser$Group,y = 8,conf.level = 0.95,conf.type = "log",hazard.type = "Nelson",plot = TRUE,plot.conf = TRUE,statistics = TRUE)
install.packages("C:/10_Package/SurvivalFast_0.1.0.zip")
cansor
canser
library(SurvivalFast)
canser
?canser
survivalFast(canser$Week,
canser$Censord,
canser$Group,
y = 8,
conf.level = 0.95,
conf.type = "log",
hazard.type = "Nelson",
plot = TRUE,
plot.conf = TRUE,
statistics = TRUE)
survivalFast(canser$Week,
canser$Censord,
canser$Group,
y = 8,
conf.level = 0.95,
conf.type = "log",
hazard.type = "Nelson",
plot = TRUE,
plot.conf = TRUE,
statistics = TRUE)
?survivalFast
install.packages("rmarkdown")
usethis::use_readme_rmd()
devtools::install_github("Lin-Wei-Han/survivalFast")
library(SurvivalFast)
summary(canser)
survivalFast(canser$Week,            # 時間(必填)
canser$Censord,         # 設限(必填)，需定義為：1 = event；0 = censor
canser$Group,           # 分組(必填)，接受兩組以上
y = 8,                  # 時間點，估計該時間點的統計量
conf.level = 0.95,      # 信心水準，計算信賴區間。預設為0.95
conf.type = "log",      # 計算信賴區間：plain(預設)；log。
hazard.type = "Nelson", # 計算累積風險函數：likelihood(預設)；Nelson。
plot = TRUE,            # 顯示圖表與否。預設為TRUE
plot.conf = TRUE,       # 顯示信賴區間與否。預設為TRUE
statistics = TRUE)      # 顯示統計量與否。預設為TRUE
survivalFast(canser$Week,            # 時間(必填)
canser$Censord,         # 設限(必填)，需定義為：1 = event；0 = censor
canser$Group,           # 分組(必填)，接受兩組以上
y = 8,                  # 時間點，估計該時間點的統計量
conf.level = 0.95,      # 信心水準，計算信賴區間。預設為0.95
conf.type = "log",      # 計算信賴區間：plain(預設)；log。
hazard.type = "Nelson", # 計算累積風險函數：likelihood(預設)；Nelson。
plot = TRUE,            # 顯示圖表與否。預設為TRUE
plot.conf = TRUE,       # 顯示信賴區間與否。預設為TRUE
statistics = TRUE)      # 顯示統計量與否。預設為TRUE
library(SurvivalFast)
canser
library(SurvivalFast)
canser
survivalFast(canser$Week,            # 時間(必填)
canser$Censord,         # 設限(必填)，需定義為：1 = event；0 = censor
canser$Group,           # 分組(必填)，接受兩組以上
y = 8,                  # 時間點，估計該時間點的統計量
conf.level = 0.95,      # 信心水準，計算信賴區間。預設為0.95
conf.type = "log",      # 計算信賴區間：plain(預設)；log。
hazard.type = "Nelson", # 計算累積風險函數：likelihood(預設)；Nelson。
plot = TRUE,            # 顯示圖表與否。預設為TRUE
plot.conf = TRUE,       # 顯示信賴區間與否。預設為TRUE
statistics = TRUE)      # 顯示統計量與否。預設為TRUE
survivalFast(canser$Week,            # 時間(必填)
canser$Censord,         # 設限(必填)，需定義為：1 = event；0 = censor
canser$Group,           # 分組(必填)，接受兩組以上
y = 8,                  # 時間點，估計該時間點的統計量
conf.level = 0.95,      # 信心水準，計算信賴區間。預設為0.95
conf.type = "log",      # 計算信賴區間：plain(預設)；log。
hazard.type = "Nelson", # 計算累積風險函數：likelihood(預設)；Nelson。
plot = TRUE,            # 顯示圖表與否。預設為TRUE
plot.conf = FALSE,       # 顯示信賴區間與否。預設為TRUE
statistics = TRUE)      # 顯示統計量與否。預設為TRUE
survivalFast(canser$Week,            # 時間(必填)
canser$Censord,         # 設限(必填)，需定義為：1 = event；0 = censor
canser$Group,           # 分組(必填)，接受兩組以上
y = 8,                  # 時間點，估計該時間點的統計量
conf.level = 0.95,      # 信心水準，計算信賴區間。預設為0.95
conf.type = "log",      # 計算信賴區間：plain(預設)；log。
hazard.type = "Nelson", # 計算累積風險函數：likelihood(預設)；Nelson。
plot = FALSE,            # 顯示圖表與否。預設為TRUE
plot.conf = TRUE,       # 顯示信賴區間與否。預設為TRUE
statistics = TRUE)      # 顯示統計量與否。預設為TRUE
canser
survivalFast(canser$Week,            # 時間(必填)
canser$Censord,         # 設限(必填)，需定義為：1 = event；0 = censor
canser$Group,           # 分組(必填)，接受兩組以上
y = 8,                  # 時間點，估計該時間點的統計量
conf.level = 0.95,      # 信心水準，計算信賴區間。預設為0.95
conf.type = "log",      # 計算信賴區間：plain(預設)；log。
hazard.type = "likelihood", # 計算累積風險函數：likelihood(預設)；Nelson。
plot = TRUE,            # 顯示圖表與否。預設為TRUE
plot.conf = TRUE,       # 顯示信賴區間與否。預設為TRUE
statistics = TRUE)      # 顯示統計量與否。預設為TRUE
survivalFast(canser$Week,            # 時間(必填)
canser$Censord,         # 設限(必填)，需定義為：1 = event；0 = censor
canser$Group,           # 分組(必填)，接受兩組以上
y = 8,                  # 時間點，估計該時間點的統計量
conf.level = 0.95,      # 信心水準，計算信賴區間。預設為0.95
conf.type = "log",      # 計算信賴區間：plain(預設)；log。
hazard.type = "Nelson", # 計算累積風險函數：likelihood(預設)；Nelson。
plot = TRUE,            # 顯示圖表與否。預設為TRUE
plot.conf = TRUE,       # 顯示信賴區間與否。預設為TRUE
statistics = TRUE)      # 顯示統計量與否。預設為TRUE
survivalFast(canser$Week,            # 時間(必填)
canser$Censord,         # 設限(必填)，需定義為：1 = event；0 = censor
canser$Group,           # 分組(必填)，接受兩組以上
y = 8,                  # 時間點，估計該時間點的統計量
conf.level = 0.95,      # 信心水準，計算信賴區間。預設為0.95
conf.type = "plain",      # 計算信賴區間：plain(預設)；log。
hazard.type = "Nelson", # 計算累積風險函數：likelihood(預設)；Nelson。
plot = TRUE,            # 顯示圖表與否。預設為TRUE
plot.conf = TRUE,       # 顯示信賴區間與否。預設為TRUE
statistics = TRUE)      # 顯示統計量與否。預設為TRUE
survivalFast(canser$Week,            # 時間(必填)
canser$Censord,         # 設限(必填)，需定義為：1 = event；0 = censor
canser$Group,           # 分組(必填)，接受兩組以上
y = 10,                  # 時間點，估計該時間點的統計量
conf.level = 0.95,      # 信心水準，計算信賴區間。預設為0.95
conf.type = "plain",      # 計算信賴區間：plain(預設)；log。
hazard.type = "Nelson", # 計算累積風險函數：likelihood(預設)；Nelson。
plot = TRUE,            # 顯示圖表與否。預設為TRUE
plot.conf = TRUE,       # 顯示信賴區間與否。預設為TRUE
statistics = TRUE)      # 顯示統計量與否。預設為TRUE
library(SurvivalFast)
canser
survivalFast(canser$Week,            # 時間(必填)
canser$Censord,         # 設限(必填)，需定義為：1 = event；0 = censor
canser$Group,           # 分組(必填)，接受兩組以上
y = 10,                  # 時間點，估計該時間點的統計量
conf.level = 0.95,      # 信心水準，計算信賴區間。預設為0.95
conf.type = "plain",      # 計算信賴區間：plain(預設)；log。
hazard.type = "Nelson", # 計算累積風險函數：likelihood(預設)；Nelson。
plot = TRUE,            # 顯示圖表與否。預設為TRUE
plot.conf = TRUE,       # 顯示信賴區間與否。預設為TRUE
statistics = TRUE)      # 顯示統計量與否。預設為TRUE
fit <- survfit(Surv(Week,Censord)~Group, data=data,conf.int=0.95)
library(survival)
fit <- survfit(Surv(Week,Censord)~Group, data=data,conf.int=0.95)
fit <- survfit(Surv(Week,Censord)~Group, data=canser,conf.int=0.95)
summary(fit)
plot(fit,xlab = "TIME",ylab = "Survival Function",col(1,2),lty=2)
plot(fit,xlab = "TIME",ylab = "Survival Function",col=c(1,2),lty=2)
plot(fit,xlab = "TIME",ylab = "Survival Function",col=c(1,2),lty=1)
fit <- survfit(Surv(Week,Censord)~Group, data=canser,conf.int=0.95)
summary(fit)
plot(fit,xlab = "TIME",ylab = "Survival Function",col=c(1,2),lty=1)
legend(25,0.9,c("Group1","Group2"),col = c(1,2),lty = 1)
canser
legend(25,0.9,c("Group1","Group2"),col = c(1,2),lty = 1,conf.int=T)
legend(25,0.9,c("Group1","Group2"),col = c(1,2),lty = 1,confint=T)
plot(fit,xlab = "TIME",ylab = "Survival Function",col=c(1,2),,confint=T,lty=1)
plot(fit,xlab = "TIME",ylab = "Survival Function",col=c(1,2),conf.int=T,lty=1)
legend(25,0.9,c("Group1","Group2"),col = c(1,2),lty = 1)
legend(30,0.9,c("Group1","Group2"),col = c(1,2),lty = 1)
legend(30,1,c("Group1","Group2"),col = c(1,2),lty = 1)
fit <- survfit(Surv(Week,Censord)~Group, data=canser,conf.int=0.95)
summary(fit)
plot(fit,xlab = "TIME",ylab = "Survival Function",col=c(1,2),conf.int=T,lty=1)
legend(30,1,c("Group1","Group2"),col = c(1,2),lty = 1)
canser
ggsurvplot(fit,canser,fun = "pct",conf.int = T,fontsize = 3)
#============================= test =================================
library(survminer)
ggsurvplot(fit,canser,fun = "pct",conf.int = T,fontsize = 3)
fit <- survfit(Surv(Week,Censord)~Group, data=canser,conf.int=0.95)
summary(fit)
plot(fit,xlab = "TIME",ylab = "Survival Function",col=c(1,2),conf.int=T,lty=1, mark.time=TRUE)
legend(30,1,c("Group1","Group2"),col = c(1,2),lty = 1)
plot(fit,xlab = "TIME",ylab = "Survival Function",col=c(1,2),conf.int=T,lty=1, mark.time=TRUE)
legend(30,1,c("Group1","Group2"),col = c(1,2),lty = 1,bty = "n")
survivalFast(canser$Week,            # 時間(必填)
canser$Censord,         # 設限(必填)，需定義為：1 = event；0 = censor
canser$Group,           # 分組(必填)，接受兩組以上
y = 10,                  # 時間點，估計該時間點的統計量
conf.level = 0.95,      # 信心水準，計算信賴區間。預設為0.95
conf.type = "plain",      # 計算信賴區間：plain(預設)；log。
hazard.type = "Nelson", # 計算累積風險函數：likelihood(預設)；Nelson。
plot = TRUE,            # 顯示圖表與否。預設為TRUE
plot.conf = TRUE,       # 顯示信賴區間與否。預設為TRUE
statistics = TRUE)      # 顯示統計量與否。預設為TRUE
?survival
library(SurvivalFast)
canser
head(canser)
library(SurvivalFast)
head(canser)
library(survival)
head(lung)
library(survival)
head(lung)
?survival
??survival
?survival
?SurvivalFast
?survivalFast
?survfit
survivalFast(canser$Week,            # 時間(必填)
canser$Censord,         # 設限(必填)，需定義為：1 = event；0 = censor
canser$Group,           # 分組(必填)，接受兩組以上
y = 10,                 # 時間點，估計該時間點的統計量
conf.level = 0.95,      # 信心水準，計算信賴區間。預設為0.95
conf.type = "plain",    # 計算信賴區間：plain(預設)；log。
hazard.type = "Nelson", # 計算累積風險函數：likelihood(預設)；Nelson。
plot = TRUE,            # 顯示圖表與否。預設為TRUE
plot.conf = TRUE,       # 顯示信賴區間與否。預設為TRUE
statistics = TRUE)      # 顯示統計量與否。預設為TRUE
fit <- survfit(Surv(Week,Censord)~Group, data=canser,conf.int=0.95)
summary(fit)
fit <- survfit(Surv(Week,Censord)~Group,
data=canser,
conf.int=0.95)
summary(fit)
plot(fit,xlab = "TIME",ylab = "Survival Function",col=c(1,2),conf.int=T,lty=1, mark.time=TRUE)
legend(30,1,c("Group1","Group2"),col = c(1,2),lty = 1,bty = "n")
plot(fit,xlab = "TIME",
ylab = "Survival Function",
col=c(1,2),
conf.int=T,
lty=1,
mark.time=TRUE)
legend(30,1,c("Group1","Group2"),
col = c(1,2),
lty = 1,bty = "n")
survdiff(Surv(Week,Censord)~Group,
data=canser)
survivalFast(canser$Week,
canser$Censord,
canser$Group,
y = 10,
conf.level = 0.95,
conf.type = "log",
hazard.type = "Nelson",
plot = TRUE,
plot.conf = TRUE,
statistics = TRUE)
devtools::load_all()
library(roxygen2)
roxygenise()
roxygenise()
roxygenise()
roxygenise()
devtools::load_all()
library(SurvivalFast)
canser
survivalFast(canser$Week,            # 時間(必填)
canser$Censord,         # 設限(必填)，需定義為：1 = event；0 = censor
canser$Group,           # 分組(必填)，接受兩組以上
y = 8,                  # 時間點，估計該時間點的統計量
conf.level = 0.95,      # 信心水準，計算信賴區間。預設為0.95
conf.type = "log",      # 計算信賴區間：plain(預設)；log。
hazard.type = "Nelson", # 計算累積風險函數：likelihood(預設)；Nelson。
plot = TRUE,            # 顯示圖表與否。預設為TRUE
plot.conf = TRUE,       # 顯示信賴區間與否。預設為TRUE
statistics = TRUE)
roxygenise()
devtools::load_all()
canser
survivalFast(canser$Week,            # 時間(必填)
canser$Censord,         # 設限(必填)，需定義為：1 = event；0 = censor
canser$Group,           # 分組(必填)，接受兩組以上
y = 8,                  # 時間點，估計該時間點的統計量
conf.level = 0.95,      # 信心水準，計算信賴區間。預設為0.95
conf.type = "log",      # 計算信賴區間：plain(預設)；log。
hazard.type = "Nelson", # 計算累積風險函數：likelihood(預設)；Nelson。
plot = TRUE,            # 顯示圖表與否。預設為TRUE
plot.conf = TRUE,       # 顯示信賴區間與否。預設為TRUE
statistics = TRUE)
devtools::load_all()
survivalFast(canser$Week,            # 時間(必填)
canser$Censord,         # 設限(必填)，需定義為：1 = event；0 = censor
canser$Group,           # 分組(必填)，接受兩組以上
y = 8,                  # 時間點，估計該時間點的統計量
conf.level = 0.95,      # 信心水準，計算信賴區間。預設為0.95
conf.type = "log",      # 計算信賴區間：plain(預設)；log。
hazard.type = "Nelson", # 計算累積風險函數：likelihood(預設)；Nelson。
plot = TRUE,            # 顯示圖表與否。預設為TRUE
plot.conf = TRUE,       # 顯示信賴區間與否。預設為TRUE
statistics = TRUE)
devtools::load_all()
library(SurvivalFast)
canser
survivalFast(canser$Week,            # 時間(必填)
canser$Censord,         # 設限(必填)，需定義為：1 = event；0 = censor
canser$Group,           # 分組(必填)，接受兩組以上
y = 8,                  # 時間點，估計該時間點的統計量
conf.level = 0.95,      # 信心水準，計算信賴區間。預設為0.95
conf.type = "log",      # 計算信賴區間：plain(預設)；log。
hazard.type = "Nelson", # 計算累積風險函數：likelihood(預設)；Nelson。
plot = TRUE,            # 顯示圖表與否。預設為TRUE
plot.conf = TRUE,       # 顯示信賴區間與否。預設為TRUE
statistics = TRUE)
devtools::load_all(".")
library(SurvivalFast)
survivalFast(canser$Week,            # 時間(必填)
canser$Censord,         # 設限(必填)，需定義為：1 = event；0 = censor
canser$Group,           # 分組(必填)，接受兩組以上
y = 8,                  # 時間點，估計該時間點的統計量
conf.level = 0.95,      # 信心水準，計算信賴區間。預設為0.95
conf.type = "log",      # 計算信賴區間：plain(預設)；log。
hazard.type = "Nelson", # 計算累積風險函數：likelihood(預設)；Nelson。
plot = TRUE,            # 顯示圖表與否。預設為TRUE
plot.conf = TRUE,       # 顯示信賴區間與否。預設為TRUE
statistics = TRUE)
devtools::load_all(".")
survivalFast(canser$Week,            # 時間(必填)
canser$Censord,         # 設限(必填)，需定義為：1 = event；0 = censor
canser$Group,           # 分組(必填)，接受兩組以上
y = 8,                  # 時間點，估計該時間點的統計量
conf.level = 0.95,      # 信心水準，計算信賴區間。預設為0.95
conf.type = "log",      # 計算信賴區間：plain(預設)；log。
hazard.type = "Nelson", # 計算累積風險函數：likelihood(預設)；Nelson。
plot = TRUE,            # 顯示圖表與否。預設為TRUE
plot.conf = TRUE,       # 顯示信賴區間與否。預設為TRUE
statistics = TRUE)
library(SurvivalFast)
survivalFast(canser$Week,            # 時間(必填)
canser$Censord,         # 設限(必填)，需定義為：1 = event；0 = censor
canser$Group,           # 分組(必填)，接受兩組以上
y = 8,                  # 時間點，估計該時間點的統計量
conf.level = 0.95,      # 信心水準，計算信賴區間。預設為0.95
conf.type = "log",      # 計算信賴區間：plain(預設)；log。
hazard.type = "Nelson", # 計算累積風險函數：likelihood(預設)；Nelson。
plot = TRUE,            # 顯示圖表與否。預設為TRUE
plot.conf = TRUE,       # 顯示信賴區間與否。預設為TRUE
statistics = TRUE)
devtools::load_all(".")
library(SurvivalFast)
survivalFast(canser$Week,            # 時間(必填)
canser$Censord,         # 設限(必填)，需定義為：1 = event；0 = censor
canser$Group,           # 分組(必填)，接受兩組以上
y = 8,                  # 時間點，估計該時間點的統計量
conf.level = 0.95,      # 信心水準，計算信賴區間。預設為0.95
conf.type = "log",      # 計算信賴區間：plain(預設)；log。
hazard.type = "Nelson", # 計算累積風險函數：likelihood(預設)；Nelson。
plot = TRUE,            # 顯示圖表與否。預設為TRUE
plot.conf = TRUE,       # 顯示信賴區間與否。預設為TRUE
statistics = TRUE)
rlang::last_trace()
roxygenise()
devtools::load_all(".")
library(SurvivalFast)
survivalFast(canser$Week,            # 時間(必填)
canser$Censord,         # 設限(必填)，需定義為：1 = event；0 = censor
canser$Group,           # 分組(必填)，接受兩組以上
y = 8,                  # 時間點，估計該時間點的統計量
conf.level = 0.95,      # 信心水準，計算信賴區間。預設為0.95
conf.type = "log",      # 計算信賴區間：plain(預設)；log。
hazard.type = "Nelson", # 計算累積風險函數：likelihood(預設)；Nelson。
plot = TRUE,            # 顯示圖表與否。預設為TRUE
plot.conf = TRUE,       # 顯示信賴區間與否。預設為TRUE
statistics = TRUE)
roxygenise()
devtools::load_all(".")
library(SurvivalFast)
survivalFast(canser$Week,            # 時間(必填)
canser$Censord,         # 設限(必填)，需定義為：1 = event；0 = censor
canser$Group,           # 分組(必填)，接受兩組以上
y = 8,                  # 時間點，估計該時間點的統計量
conf.level = 0.95,      # 信心水準，計算信賴區間。預設為0.95
conf.type = "log",      # 計算信賴區間：plain(預設)；log。
hazard.type = "Nelson", # 計算累積風險函數：likelihood(預設)；Nelson。
plot = TRUE,            # 顯示圖表與否。預設為TRUE
plot.conf = TRUE,       # 顯示信賴區間與否。預設為TRUE
statistics = TRUE)
survivalFast(canser$Week,            # 時間(必填)
canser$Censord,         # 設限(必填)，需定義為：1 = event；0 = censor
canser$Group,           # 分組(必填)，接受兩組以上
y = 8,                  # 時間點，估計該時間點的統計量
conf.level = 0.95,      # 信心水準，計算信賴區間。預設為0.95
conf.type = "log",      # 計算信賴區間：plain(預設)；log。
hazard.type = "Nelson", # 計算累積風險函數：likelihood(預設)；Nelson。
plot = TRUE,            # 顯示圖表與否。預設為TRUE
plot.conf = TRUE,       # 顯示信賴區間與否。預設為TRUE
statistics = TRUE)
roxygenise()
devtools::load_all(".")
library(SurvivalFast)
survivalFast(canser$Week,            # 時間(必填)
canser$Censord,         # 設限(必填)，需定義為：1 = event；0 = censor
canser$Group,           # 分組(必填)，接受兩組以上
y = 8,                  # 時間點，估計該時間點的統計量
conf.level = 0.95,      # 信心水準，計算信賴區間。預設為0.95
conf.type = "log",      # 計算信賴區間：plain(預設)；log。
hazard.type = "Nelson", # 計算累積風險函數：likelihood(預設)；Nelson。
plot = TRUE,            # 顯示圖表與否。預設為TRUE
plot.conf = TRUE,       # 顯示信賴區間與否。預設為TRUE
statistics = TRUE)
devtools::load_all(".")
roxygenise()
devtools::load_all(".")
library(SurvivalFast)
survivalFast(canser$Week,            # 時間(必填)
canser$Censord,         # 設限(必填)，需定義為：1 = event；0 = censor
canser$Group,           # 分組(必填)，接受兩組以上
y = 8,                  # 時間點，估計該時間點的統計量
conf.level = 0.95,      # 信心水準，計算信賴區間。預設為0.95
conf.type = "log",      # 計算信賴區間：plain(預設)；log。
hazard.type = "Nelson", # 計算累積風險函數：likelihood(預設)；Nelson。
plot = TRUE,            # 顯示圖表與否。預設為TRUE
plot.conf = TRUE,       # 顯示信賴區間與否。預設為TRUE
statistics = TRUE)
roxygenise()
devtools::load_all(".")
library(SurvivalFast)
survivalFast(canser$Week,            # 時間(必填)
canser$Censord,         # 設限(必填)，需定義為：1 = event；0 = censor
canser$Group,           # 分組(必填)，接受兩組以上
y = 8,                  # 時間點，估計該時間點的統計量
conf.level = 0.95,      # 信心水準，計算信賴區間。預設為0.95
conf.type = "log",      # 計算信賴區間：plain(預設)；log。
hazard.type = "Nelson", # 計算累積風險函數：likelihood(預設)；Nelson。
plot = TRUE,            # 顯示圖表與否。預設為TRUE
plot.conf = TRUE,       # 顯示信賴區間與否。預設為TRUE
statistics = TRUE)
roxygenise()
devtools::load_all(".")
library(SurvivalFast)
survivalFast(canser$Week,            # 時間(必填)
canser$Censord,         # 設限(必填)，需定義為：1 = event；0 = censor
canser$Group,           # 分組(必填)，接受兩組以上
y = 8,                  # 時間點，估計該時間點的統計量
conf.level = 0.95,      # 信心水準，計算信賴區間。預設為0.95
conf.type = "log",      # 計算信賴區間：plain(預設)；log。
hazard.type = "Nelson", # 計算累積風險函數：likelihood(預設)；Nelson。
plot = TRUE,            # 顯示圖表與否。預設為TRUE
plot.conf = TRUE,       # 顯示信賴區間與否。預設為TRUE
statistics = TRUE)
devtools::load_all(".")
utils::globalVariables(c("Censord", "Group", "Week"))
devtools::load_all(".")
library(SurvivalFast)
survivalFast(canser$Week,            # 時間(必填)
canser$Censord,         # 設限(必填)，需定義為：1 = event；0 = censor
canser$Group,           # 分組(必填)，接受兩組以上
y = 8,                  # 時間點，估計該時間點的統計量
conf.level = 0.95,      # 信心水準，計算信賴區間。預設為0.95
conf.type = "log",      # 計算信賴區間：plain(預設)；log。
hazard.type = "Nelson", # 計算累積風險函數：likelihood(預設)；Nelson。
plot = TRUE,            # 顯示圖表與否。預設為TRUE
plot.conf = TRUE,       # 顯示信賴區間與否。預設為TRUE
statistics = TRUE)
devtools::install_github("Lin-Wei-Han/survivalFast")
library(SurvivalFast)
canser
?canser
?survivalFast
survivalFast(canser$Week,            # 時間(必填)
canser$Censord,         # 設限(必填)，需定義為：1 = event；0 = censor
canser$Group,           # 分組(必填)，接受兩組以上
y = 8,                  # 時間點，估計該時間點的統計量
conf.level = 0.95,      # 信心水準，計算信賴區間。預設為0.95
conf.type = "log",      # 計算信賴區間：plain(預設)；log。
hazard.type = "Nelson", # 計算累積風險函數：likelihood(預設)；Nelson。
plot = TRUE,            # 顯示圖表與否。預設為TRUE
plot.conf = TRUE,       # 顯示信賴區間與否。預設為TRUE
statistics = TRUE)      # 顯示統計量與否。預設為TRUE
devtools::load_all(".")
remove.packages("SurvivalFast")
devtools::install_github("Lin-Wei-Han/survivalFast")
canser
survivalFast(canser$Week,            # 時間(必填)
canser$Censord,         # 設限(必填)，需定義為：1 = event；0 = censor
canser$Group,           # 分組(必填)，接受兩組以上
y = 8,                  # 時間點，估計該時間點的統計量
conf.level = 0.95,      # 信心水準，計算信賴區間。預設為0.95
conf.type = "log",      # 計算信賴區間：plain(預設)；log。
hazard.type = "Nelson", # 計算累積風險函數：likelihood(預設)；Nelson。
plot = TRUE,            # 顯示圖表與否。預設為TRUE
plot.conf = TRUE,       # 顯示信賴區間與否。預設為TRUE
statistics = TRUE)      # 顯示統計量與否。預設為TRUE
